<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.googlemapsapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpecialOfferDetailActivity"></activity>
        <activity android:name=".MentalHealthModulePlaceholder" />
        <activity android:name=".NutritionModulePlaceholder" />
        <activity android:name=".ExcerciseModulePlaceholder" />
        <activity android:name=".MentalHealthModuleFragments.MentalHealthQuizFailedActivity" />
        <activity android:name=".MentalHealthModuleFragments.MentalHealthQuizPassedActivity" />
        <activity android:name=".MentalHealthModuleFragments.MentalHealthQuizActivity" />
        <activity android:name=".ExcerciseModuleFragments.ExcerciseModuleQuizPassedActivity" />
        <activity android:name=".ExcerciseModuleFragments.ExcerciseModuleQuizFailedActivity" />
        <activity android:name=".NutritionModuleFragments.NutritionModuleQuizPassedActivity" />
        <activity android:name=".NutritionModuleFragments.NutritionModuleQuizFailedActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".NutritionModuleFragments.PlateBuilder" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB6iw5s4YJZD2Dgw4uY8Ec53dfhy16sPJE" />

        <activity
            android:name=".ExcerciseModuleFragments.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>